# Task: Backend Initialization (Phase 1)
**Assignee:** Nong Back (back)
**Status:** Pending

## Objectives
1. Initialize Django project in `backend/`.
2. Install `django-tenants`.
3. Setup `shared_schema` (Client, Domain) and `tenant_schema`.
4. Create necessary `requirements.txt` or `Pipfile`.
5. Ensure it runs with `python manage.py runserver`.

## Deliverables
- Working Django project structure.
- `settings.py` configured for django-tenants.
- Initial migrations applied.
