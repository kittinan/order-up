# Task: Infrastructure Setup (Phase 1)
**Assignee:** Phi Deploy (deploy)
**Status:** Pending

## Objectives
1. Review and update `docker-compose.yml`.
2. Create `Dockerfile` for `backend/`.
3. Create `Dockerfile` for `frontend/`.
4. Ensure all services (db, redis, backend, frontend) start correctly.

## Deliverables
- `docker-compose.yml` working.
- Dockerfiles optimized for dev.
- `make` or script to start the stack easily (optional).
